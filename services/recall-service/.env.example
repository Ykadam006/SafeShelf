PORT=5001
NODE_ENV=development
OPENFDA_API_KEY=

# Production (Vercel) — set on the safeshelf-recall project:
#   NODE_ENV=production
#   OPENFDA_API_KEY=your_openfda_key   (optional; raises rate limits)
# Vercel manages PORT automatically — do not set it there.
