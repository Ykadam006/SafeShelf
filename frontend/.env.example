# Local development (api-service runs on 5000 by default; we use 5002 if 5000 is taken on macOS).
VITE_API_BASE_URL=http://localhost:5000/api

# Production (Vercel) — set this in the safe-shelf project's Environment Variables:
# VITE_API_BASE_URL=https://safeshelf-api.vercel.app/api
